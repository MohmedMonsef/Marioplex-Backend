dist: bionic
sudo: false
language: node_js
install: 
 - git clone https://github.com/MohmedMonsef/Marioplex-Backend
script:
  - true
notifications:
  email:
    recipients:
      - belalelhossany12@gmail.com
before_deploy: 
- eval "$(ssh-agent -s)"
- chmod 600 key.pem
- ssh-add key.pem
- ssh -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -i ./key.pem ubuntu@ec2-52-205-254-29.compute-1.amazonaws.com
deploy:
 provider: script
 skip_cleanup: true
 script: bash Deploy.sh
